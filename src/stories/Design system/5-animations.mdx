import { Meta } from '@storybook/blocks';

<Meta title="Design System/Animations" />

export const H2 = ({ children }) => (
  <h2 style={{ color: '#ff0036', fontSize: '1.125rem', fontWeight: 700, paddingBottom: '12px', marginTop: '2rem', marginBottom: '1rem', borderBottom: '1px solid #172230' }}>
    {children}
  </h2>
);

# Animations

<p style={{ color: '#cccccc', marginBottom: '0.5rem' }}>
  Animation tokens defined in <code>src/styles/theme.css</code>. Hover each preview to trigger the animation.
</p>
<p style={{ color: '#888888', fontSize: '13px', marginBottom: '2rem' }}>
  All transitions respect <code>prefers-reduced-motion</code>. Max duration: <strong style={{ color: '#cccccc' }}>560ms</strong>. Only animate <code>opacity</code>, <code>transform</code>, and <code>box-shadow</code> — never layout properties.
</p>

<H2>Fade</H2>

<div style={{ display: 'flex', flexDirection: 'column', gap: '1.5rem', marginBottom: '2rem' }}>
  <div style={{ display: 'flex', alignItems: 'center', gap: '1.5rem' }}>
    <div style={{ border: '1px solid #172230', width: '48px', height: '48px', borderRadius: '50%', overflow: 'hidden', display: 'flex', justifyContent: 'center', alignItems: 'center', background: '#0B131E', flexShrink: 0 }}>
      <div className="hover:animate-fadeIn" style={{ width: '32px', height: '32px', borderRadius: '50%', backgroundColor: '#ff0036', opacity: '0', transition: '0.4s ease-in-out', animationFillMode: 'forwards' }}></div>
    </div>
    <div>
      <code style={{ color: '#ffffff', fontSize: '13px' }}>--animate-fadeIn</code>
      <div style={{ color: '#888888', fontSize: '12px', marginTop: '2px' }}>fadeIn 400ms ease-in-out</div>
    </div>
  </div>

  <div style={{ display: 'flex', alignItems: 'center', gap: '1.5rem' }}>
    <div style={{ border: '1px solid #172230', width: '48px', height: '48px', borderRadius: '50%', overflow: 'hidden', display: 'flex', justifyContent: 'center', alignItems: 'center', background: '#0B131E', flexShrink: 0 }}>
      <div className="hover:animate-fadeOut" style={{ width: '32px', height: '32px', borderRadius: '50%', backgroundColor: '#ff0036', opacity: '1', transition: '0.4s ease-in-out', animationFillMode: 'forwards' }}></div>
    </div>
    <div>
      <code style={{ color: '#ffffff', fontSize: '13px' }}>--animate-fadeOut</code>
      <div style={{ color: '#888888', fontSize: '12px', marginTop: '2px' }}>fadeOut 400ms ease-in-out</div>
    </div>
  </div>
</div>

<H2>Rotation</H2>

<div style={{ display: 'flex', flexDirection: 'column', gap: '1.5rem', marginBottom: '2rem' }}>
  <div style={{ display: 'flex', alignItems: 'center', gap: '1.5rem' }}>
    <div className="hover:animate-rotation-360" style={{ width: '48px', height: '48px', background: 'linear-gradient(135deg, #ff1a4b 0%, #cc0030 100%)', borderRadius: '8px', flexShrink: 0 }}></div>
    <div>
      <code style={{ color: '#ffffff', fontSize: '13px' }}>--animate-rotation-360</code>
      <div style={{ color: '#888888', fontSize: '12px', marginTop: '2px' }}>rotation-360 1s linear infinite</div>
    </div>
  </div>

  <div style={{ display: 'flex', alignItems: 'center', gap: '1.5rem' }}>
    <div className="hover:animate-rotation-360-reverse" style={{ width: '48px', height: '48px', background: 'linear-gradient(135deg, #ff1a4b 0%, #cc0030 100%)', borderRadius: '8px', flexShrink: 0 }}></div>
    <div>
      <code style={{ color: '#ffffff', fontSize: '13px' }}>--animate-rotation-360-reverse</code>
      <div style={{ color: '#888888', fontSize: '12px', marginTop: '2px' }}>rotation-360 1s linear infinite reverse</div>
    </div>
  </div>
</div>

<H2>Transition Tokens</H2>

<div style={{ display: 'flex', flexDirection: 'column', gap: '0.5rem', marginBottom: '3rem' }}>
  {[
    { token: '--transition-fast',      value: '150ms ease-in-out',                        use: 'Micro-interactions, tooltips' },
    { token: '--transition-base',      value: '250ms ease',                               use: 'Default — buttons, hover states' },
    { token: '--transition-slow',      value: '300ms ease-in-out',                        use: 'Panel open/close, modals' },
    { token: '--transition-enter',     value: '560ms cubic-bezier(.2,.8,.2,1)',            use: 'Section fade-in on scroll' },
    { token: '--transition-btn',       value: 'box-shadow 250ms ease, background 250ms ease', use: 'Button hover transitions' },
    { token: '--transition-link',      value: 'color 200ms ease',                         use: 'Link color on hover' },
    { token: '--transition-border',    value: 'border-color 200ms ease, box-shadow 200ms ease', use: 'Input focus transitions' },
  ].map(({ token, value, use }) => (
    <div key={token} style={{ display: 'flex', gap: '1rem', padding: '0.5rem 0', borderBottom: '1px solid #172230', fontSize: '13px', alignItems: 'baseline' }}>
      <code style={{ color: '#cccccc', minWidth: '200px' }}>{token}</code>
      <span style={{ color: '#ff335e', minWidth: '300px', fontFamily: 'monospace', fontSize: '12px' }}>{value}</span>
      <span style={{ color: '#888888' }}>{use}</span>
    </div>
  ))}
</div>

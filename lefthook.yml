pre-commit:
  parallel: false
  commands:
    biome:
      glob: "**/*.{ts,tsx,js,jsx,css}"
      run: "biome check --write --staged {staged_files}"
    typecheck:
      run: "pnpm exec tsc --noEmit"
